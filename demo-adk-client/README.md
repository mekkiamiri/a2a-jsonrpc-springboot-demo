# demo-adk-client

## Building the project

To build the project, run the following command:

```bash
./mvnw clean install
```

## Testing the project

To test the project, you need to have the `demo-adk-server` running.

Once the server is running, you can run the tests with the following command:

```bash
./mvnw spring-boot:run
```
